/** Runtime global for Durable Object WebSocket hibernation auto-response. */
declare class WebSocketRequestResponsePair {
  constructor(request: string | ArrayBuffer, response: string | ArrayBuffer);
}

export {};
