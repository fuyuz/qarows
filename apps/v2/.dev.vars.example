# Copy to .dev.vars for local `wrangler dev` (optional).
#
# Phase 2 does not require secrets for local development.
# Auth in production uses Cloudflare Access (Cf-Access-Authenticated-User-Email).
# Local dev accepts optional X-Qarows-User request header, or falls back to dev@local.
